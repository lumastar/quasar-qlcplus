language: minimal

sudo: required

services:
  - docker

script:
  - ./build.sh

deploy:
  provider: releases
  api_key: "$GITHUB_API_TOKEN"
  file: "raspbian-setup-$TRAVIS_TAG.zip"
  skip_cleanup: true
  on:
    tags: true